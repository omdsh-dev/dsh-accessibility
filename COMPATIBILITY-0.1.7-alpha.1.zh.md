# DSH 0.1.7-alpha.1 兼容适配

[English](COMPATIBILITY-0.1.7-alpha.1.md) | 简体中文

状态：候选版本的本地自动化检查通过；**尚未发布，真实辅助技术验证仍待完成**。核对日期：2026-09-22。

## 配套插件本地检查

候选版本 `0.1.2-alpha.1` 将全部 DSH 依赖固定到 `0.1.7-alpha.1`。本地已通过主机端/客户端类型检查、构建，以及 28 个文件中的 234 项测试。测试覆盖完整锁定依赖图、输入状态中的排队数量、显式载入内容、多阅读视图独立状态、唯一标识、焦点恢复和实验室精确版本门禁。这些是组件测试，不是真实读屏观察。

核心迁移已提交为 `feat/a11y-core-0.1.7-alpha.1` 分支的 `b11b37b065`。外部插件联调在该核心提交上通过了五项测试，包括 Chromium/Firefox/WebKit 环境检查。最终核心 GUI 测试通过 7,396 项、跳过一项；文档检查的 42 个门禁全部通过。核心无障碍浏览器检查在 Chromium 通过 14 项，在 Firefox/WebKit 各通过 13 项，后两者各跳过一项不受支持的强制颜色检查。完整只读 Web 回放通过 486 项、按条件跳过 15 项，共 143 个文件，其中 142 个通过、一个跳过。语料检查通过，历史代际保留，共享引用已指向属主选中的代际。npm 打包预检通过。插件与核心实验室的启动、清理，以及六种实时实验场景启动均通过无界面冒烟测试；没有真人操作辅助技术。发布和真实 VoiceOver/NVDA/用户观察仍待完成。初次失败记录与最终通过结果分开保留，没有将失败记录改称通过。

## 远端 CI 与后续候选

插件的[首轮远端验证](https://github.com/omdsh-dev/dsh-accessibility/actions/runs/35708472204)已通过 macOS/Windows/Linux × Node 22/24 六组矩阵，以及基于核心 `b11b37b065` 的组装浏览器检查。核心无障碍浏览器检查、Node 兼容、原生构建和打包也已在远端通过。这不代表核心完整 CI 已通过。

核心后续提交 `41074ff082` 使用新标签 `dsh-v0.1.7-alpha.1-a11y.2`，修复首轮发现的旧 Windows 测试路径、仅属于上游的 Project 策略凭据、被新版规范禁止的测试类型断言，以及空闲窗口未随测试迁移的心跳夹具，保留断言和历史证据。124 项定向测试通过；最终单独终端回归通过 20 项，修正后的 headless 心跳场景通过。本地 59 项静态门禁中，57 项在整轮通过，另外两项修复后单独复查通过；主机端/客户端类型检查通过。插件的[后续验证](https://github.com/omdsh-dev/dsh-accessibility/actions/runs/35710243579)已通过六组平台/运行时检查、组装检查和汇总检查，[PR #27](https://github.com/omdsh-dev/dsh-accessibility/pull/27)已合入 `main`。[第二轮核心检查](https://github.com/omdsh-dev/deepseek-harness/actions/runs/35710192223)的覆盖率与 Web 检查仍失败，因此没有将核心视为可合入状态。

核心候选 `dsh-v0.1.7-alpha.1-a11y.3` 固定在 `2f7eaf7865`。此候选修复子代理目录触发器重复处理 ArrowUp/ArrowDown 的问题，并补齐焦点与 JSON 模式冲突的覆盖率测试。测试夹具遵循当前 PowerShell 提示符协议，确保 Windows 跨盘符情况下仍使用相对路径，自行控制 ACL 继承，并独立于会话树是否有内容来判断客户端注册表就绪。本地通过 279 项定向测试，五个对应源码文件覆盖率均为 100%；GUI 通过 7,398 项、跳过一项；只读 Web 回放通过 486 项、按条件跳过 15 项。使用校验和核验后的临时 PowerShell 运行时，在 macOS 上通过三项真实 PowerShell 测试。最初一次 Firefox 焦点检查在本地验证交叠期间失败，随后诊断复跑与最终独立三浏览器检查通过；独立结果为 Chromium 通过 14 项，Firefox/WebKit 各通过 13 项、各跳过一项不受支持的强制颜色检查。这些结果不是真实辅助技术使用认证。当前远端结果以[核心 PR #4](https://github.com/omdsh-dev/deepseek-harness/pull/4)为准，合入目标是 `baseline/dsh-v0.1.7-alpha.1`，不是较旧的 `master`；包括 Windows 覆盖率在内的全部远端检查通过后才可合入。npm 发布是独立步骤，本次尚未执行。

在[第三轮核心检查](https://github.com/omdsh-dev/deepseek-harness/actions/runs/35715728279)中，24 项检查通过，包括 Linux 覆盖率、Web 回放；Windows ACL 运行器测试也已通过，但 Windows 覆盖率因另外两处测试约束失败。账户 Cookie 测试要求并发的资料和余额请求按固定顺序到达；全仓编译器扫描耗时 65 秒，超过测试自行设置的 60 秒上限，而该任务已有 90 秒预算。核心 `0f8480ec06` 使用新标签 `dsh-v0.1.7-alpha.1-a11y.4`，保留精确请求数量、登录/退出的串行顺序、Cookie、令牌/隐私断言及模式拒绝检查，只允许两条并发请求以任意顺序到达，并将编译器测试恢复到任务预算。本地 112 项定向测试通过。插件 CI 现固定到第四个候选，之前的标签和失败记录保持不变；完整远端合入条件和真实辅助技术待验证状态不变。

## 固定上游目标

GitHub 最新预发布版为 [dsh-v0.1.7-alpha.1](https://github.com/deepseek-ai/deepseek-harness/releases/tag/dsh-v0.1.7-alpha.1)，发布时间为 2026-09-22 06:16:27 UTC，源码修订为 `c36a83ff6bb95e3f82cf79f9be7c724270a8aa61`。它替代 0.1.6-alpha.2 成为预览版迁移目标，但不改变任何已有证据的版本归属。

核对期间，npm 官方源的标签从 `alpha=0.1.6-alpha.2` 更新为 `alpha=0.1.7-alpha.1`。最后核验的标签为 `latest=0.1.5-rc.2`、`next=0.1.5-rc.3`、`alpha=0.1.7-alpha.1`。GitHub 发布和 npm 分发需要分别检查；安装、固定依赖或发布前必须重新核验完整依赖图。

保留正在进行的 0.1.5-rc.2 核心及配套插件迁移。它的测试通过不代表兼容 0.1.7；现有 beta.8 和真人验证活动的证据仍绑定各自原始版本。

## 迁移与验收事项

- **Session V4：**使用受支持的后继代际调整隔离实验夹具，保留历史日志和证据；检查历史加载、取消的回答、附件及复制结果。
- **对话投影与多实例：**核对 Accessible View 使用的当前 slot 所有者、节点数据源、新增的 `turnDataSource` 及记录字段变化。多个对话同时挂载时，验证会话绑定、卸载、显式载入内容及焦点恢复。
- **设置与插件生命周期：**上游更换了设置持久化机制，并加入配置表单。当前配套插件使用增量设置分区，而非旧持久化服务，应验证实际集成，不添加无关兼容层。检查安装、安装失败、禁用、卸载、重新启用及多语言。
- **核心语义补丁：**移植到当前所属组件；围绕重组后的过程折叠和对话框架，复测地标、输入名称、焦点顺序、对话框、复合控件、面板尺寸调整和持久状态播报。
- **新增任务界面：**覆盖停止并归档确认、撤销归档、置顶及筛选、后台任务输出、文件操作、左右差异审阅、表格导航、缩放和实验性转写控件。支持声明只能覆盖实际验证过的任务。
- **宿主接口变化：**扩大创作工具或页面提供方的兼容声明前，检查旧工作区读取及 Remote 接口使用；保持原有权限、同意及隐私边界。
- **发布证据：**预览版发布前，要求完整且版本一致的 npm 依赖图，类型、单元、构建和包检查，以及固定干净修订的 Chromium、Firefox、WebKit 集成结果；真人辅助技术与用户验证缺口必须明确保留。自动化门禁通过后才能单独发布 alpha 候选，不提升为稳定版，不复用 0.1.5 的结果。

真实 VoiceOver/NVDA 和残障开发者观察仍待提交。本记录是迁移计划，不是无障碍验证证据或认证。
